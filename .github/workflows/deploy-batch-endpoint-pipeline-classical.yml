name: deploy-batch-endpoint-pipeline

on:
  push:
  workflow_dispatch:

jobs:
  set-env:
    runs-on: ubuntu-latest
    outputs:
      config-file: ${{ steps.set-output-defaults.outputs.config-file }}
    steps:
      - id: set-prod-branch
        name: Set prod branch
        if: ${{ github.ref == 'refs/heads/main' }}
        run: echo "config_env=config-infra-prod.yml" >> "$GITHUB_ENV"

      - id: set-dev-branch
        name: Set dev branch
        if: ${{ github.ref != 'refs/heads/main' }}
        run: echo "config_env=config-infra-dev.yml" >> "$GITHUB_ENV"

      - id: set-output-defaults
        name: Set output defaults
        run: echo "config-file=$config_env" >> "$GITHUB_OUTPUT"

  parse-config:
    needs: set-env
    runs-on: ubuntu-latest
    outputs:
      resource_group: ${{ steps.config.outputs.resource_group }}
      aml_workspace: ${{ steps.config.outputs.aml_workspace }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Parse config
        id: config
        uses: ./.github/actions/parse-config
        with:
          config-file: ${{ needs.set-env.outputs.config-file }}

  deploy:
    needs: [set-env, parse-config]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Create batch compute cluster
        run: |
          az ml compute create \
            --name batch-cluster \
            --type amlcompute \
            --size STANDARD_DS3_V2 \
            --min-instances 0 \
            --max-instances 5 \
            --tier low_priority \
            --resource-group ${{ needs.parse-config.outputs.resource_group }} \
            --workspace-name ${{ needs.parse-config.outputs.aml_workspace }} \
            || true

      - name: Create batch endpoint
        run: |
          az ml batch-endpoint create \
            --file mlops/azureml/deploy/batch/batch-endpoint.yml \
            --name taxi-batch-endpoint \
            --resource-group ${{ needs.parse-config.outputs.resource_group }} \
            --workspace-name ${{ needs.parse-config.outputs.aml_workspace }}

      - name: Create batch deployment
        run: |
          az ml batch-deployment create \
            --file mlops/azureml/deploy/batch/batch-deployment.yml \
            --endpoint-name taxi-batch-endpoint \
            --resource-group ${{ needs.parse-config.outputs.resource_group }} \
            --workspace-name ${{ needs.parse-config.outputs.aml_workspace }}
